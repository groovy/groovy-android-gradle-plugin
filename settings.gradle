include 'gradle-groovy-android-plugin'
include 'groovy-android-sample-app'

rootProject.name = 'groovy-android'

def setBuildFile(projects) {
  projects.each {
    it.buildFileName = "${it.name}.gradle" - 'groovy-android-'
    setBuildFile(it.children)
  }
}

setBuildFile(rootProject.children)
